/** /rfq/:productId — Bulk RFQ. A shopkeeper needing 200 gamchas cannot express that on Amazon. Routed to the artisan as a VOICE notification with the cluster copied. */
export default function Page() {
  return (
    <div className="wrap">
      <h1>/rfq/:productId</h1>
      <p className="stub">Bulk RFQ. A shopkeeper needing 200 gamchas cannot express that on Amazon. Routed to the artisan as a VOICE notification with the cluster copied.</p>
    </div>
  );
}
