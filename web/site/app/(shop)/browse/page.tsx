/** /browse — Filter by craft, cluster, GI tag, material. */
export default function Page() {
  return (
    <div className="wrap">
      <h1>/browse</h1>
      <p className="stub">Filter by craft, cluster, GI tag, material.</p>
    </div>
  );
}
