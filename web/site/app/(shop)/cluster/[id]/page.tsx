/** /cluster/:id — Cluster page — the Common Facility Centre and the crafts it covers. */
export default function Page() {
  return (
    <div className="wrap">
      <h1>/cluster/:id</h1>
      <p className="stub">Cluster page — the Common Facility Centre and the crafts it covers.</p>
    </div>
  );
}
