/** /admin/artisans — Multi-tenant artisan list. Each agency sees only its own. */
export default function Page() {
  return (
    <div className="wrap">
      <h1>/admin/artisans</h1>
      <p className="stub">Multi-tenant artisan list. Each agency sees only its own.</p>
    </div>
  );
}
