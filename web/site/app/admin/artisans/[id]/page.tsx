/** /admin/artisans/:id — Readiness flags, catalog, channel status, call log. */
export default function Page() {
  return (
    <div className="wrap">
      <h1>/admin/artisans/:id</h1>
      <p className="stub">Readiness flags, catalog, channel status, call log.</p>
    </div>
  );
}
