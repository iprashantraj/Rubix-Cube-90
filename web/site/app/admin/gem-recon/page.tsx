/** /admin/gem-recon — GeM has no order API. A coordinator reads the GeM dashboard and marks orders here. We show the gap rather than faking sync. */
export default function Page() {
  return (
    <div className="wrap">
      <h1>/admin/gem-recon</h1>
      <p className="stub">GeM has no order API. A coordinator reads the GeM dashboard and marks orders here. We show the gap rather than faking sync.</p>
    </div>
  );
}
