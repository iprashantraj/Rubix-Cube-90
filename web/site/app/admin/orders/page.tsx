/** /admin/orders — Order board across every artisan in the tenant. */
export default function Page() {
  return (
    <div className="wrap">
      <h1>/admin/orders</h1>
      <p className="stub">Order board across every artisan in the tenant.</p>
    </div>
  );
}
