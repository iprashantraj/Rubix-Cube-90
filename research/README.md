# research/

Experiments that decide what `ai/` ships. Messy is fine here.

**Nothing in `ai/` imports from `research/`.** This folder proves a choice; `ai/` runs it.
When an experiment wins, port the code across and log the verdict in `RESULTS.md`.

`data/` is gitignored — commit a manifest describing the sets, not the files.
