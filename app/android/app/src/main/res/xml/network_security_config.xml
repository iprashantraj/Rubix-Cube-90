<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is permitted for loopback ONLY, so a device build can reach a dev API
  through `adb reverse tcp:8000 tcp:8000` over USB.

  Deliberately NOT a blanket `cleartextTrafficPermitted="true"`, and deliberately not a
  LAN subnet: loopback over adb cannot be reached by anything else on the network, so this
  cannot silently become a plaintext channel in the field. Production traffic is HTTPS and
  is covered by base-config below.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
